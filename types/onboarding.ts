
export type OnboardingSlide = {
  id: string;
  image: string;
  subtitle: string;
  title: string;
};

export type GoalOption = {
  description: string;
  icon: string;
  id: string;
  label: string;
};
